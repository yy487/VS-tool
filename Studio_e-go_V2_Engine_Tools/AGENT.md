# 月神楽 AGENT 规则

## 项目类型

- 本目录按逆向项目处理。
- 当前目标是确认 `Studio_e-go_V2` 的资源包链与脚本链。
- 当前阶段已按困难脚本转向策略切到“文本优先”。
- 当前不把完整 `.scr` 指令集重构当阶段阻塞项。

## 当前已确认事实

- 样本目录在 `game/`
- 存在 `game00.dat` ~ `game04.dat`
- 引擎导出中存在 `Game%02d.dat`
- 引擎导出中存在 `Script.dat`
- 引擎导出中存在 `script\%s`
- `script\%s` 当前已确认是运行时脚本资源路径模板
- 包内当前已确认存在正式 `script` 组
- `BtText.dat` 已能解析到 `TXT0` 字符串池
- `tiNameSp.dat` 已能正式反编译 / 回编 / 目标编码写回
- 引擎标识为 `TE_V2`

## 工作边界

- 根目录只保留正式 CLI 入口。
- 复杂实现沉到 `script/`、`archive/`、`common/`。
- `analysis/` 只放探针、IDA 结论、实验稿，不作为正式入口依赖。
- `game/` 不进入正式提交。
- `ida-reverse/` 不默认进入正式提交。
- 当前优先处理文本主载体、可变长文本、自洽回写。
- 当前不要把完整 `.scr` IR / CFG 作为默认主线。
